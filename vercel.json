{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && cd server && npm install --include=dev && npx prisma generate && cd ../client && npm install --include=dev",
  "buildCommand": "cd server && npx prisma migrate deploy && npm run migrate:like-credits && npm run build && cd ../client && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "memory": 1024,
      "includeFiles": "server/node_modules/.prisma/**"
    }
  }
}
